extends layout

block content
	.w3-container
		.w3-card-4
			header.w3-container.w3-blue-grey
				h3 Autorização de Uso de Conta
			.w3-container
				p Olá.
				p A aplicação !{client.name} está a pedir acesso à sua conta.
				p Aprova o acesso?
				p ID da transação: !{transactionID}
				form(action='/oauth/dialog/authorize/decision' method='post')
					input(name='transaction_id' type='hidden' value= transactionID)
					div
						input(type='submit' value='Allow')
						|  
						input(type='submit' value='Deny' name='cancel')
					br
		footer.w3-container.w3-blue-grey
			a(href="/") Voltar à página principal